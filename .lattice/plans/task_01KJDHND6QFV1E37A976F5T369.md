# RP-17: Remove loading spinner

RP-1 added a loading spinner as an example task. The final design (see docs/mockups/LOAD SCREEN.png) has no spinner — the globe just loads. Remove the spinner markup and any JS that shows/hides it. Should be done as part of or after RP-4 (arch refactor) since the init flow will change anyway.
