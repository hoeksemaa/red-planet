# RP-1: Add a loading spinner while the globe initializes

Show a centered CSS spinner in index.html that hides once the Three.js scene is ready. No dependencies — pure CSS animation, removed on first render frame.
