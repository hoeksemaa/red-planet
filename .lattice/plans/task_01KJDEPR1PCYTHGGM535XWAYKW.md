# RP-10: Compass widget

A compass rose that rotates to reflect the camera's current heading. Fixed position (top-right or similar). Clicking it resets north-up orientation. Styled per RP-6 (Google Maps tokens). Should update via postRender listener on camera heading.
